# 本仓是公开仓、在 GitHub Actions 上构建,lockfile 必须公网可解析。
# 不加这行的话,配了公司内网 registry 的机器(~/.npmrc)跑 bun install 会把
# https://git.5th.im/... 写进 bun.lock,runner 连不上内网,bun install 会无限
# 挂起而不是报错 —— 2026-07-30 到 09-24 的 100 次 Actions 全军覆没就是这么来的。
registry=https://registry.npmjs.org/
